# ========================================================
# frontend/.env.example
# Skopíruj tento súbor ako  frontend/.env  a uprav podľa potreby.
#   cp .env.example .env
# ========================================================

# URL backend API. Musí zodpovedať portu, na ktorom beží backend (PORT v backend/.env).
REACT_APP_API_URL=http://localhost:5000
