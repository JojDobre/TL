<%- include('partials/auth-head', { title: 'Nastav nové heslo', pageStyle: `
  .auth { min-height:100vh; display:grid; grid-template-columns: 1.05fr 1fr; }
  .auth-brand { position:relative; overflow:hidden; padding:48px; display:flex; flex-direction:column; justify-content:space-between;
    background: linear-gradient(150deg,#140a2e,#1d1140 50%,#0a1130); border-right:1px solid var(--line); }
  .auth-brand::before { content:""; position:absolute; inset:0; background:
    radial-gradient(600px 400px at 20% 10%, rgba(124,92,255,.35), transparent 60%),
    radial-gradient(500px 360px at 90% 90%, rgba(250,204,21,.14), transparent 55%); }
  .auth-brand > * { position:relative; z-index:1; }
  .auth-logo { display:flex; align-items:center; gap:11px; }
  .auth-tagline { font-family:var(--font-display); font-weight:700; font-size:clamp(2rem,3vw,2.9rem); line-height:1.05; letter-spacing:-.02em; color:#fff; }
  .auth-feat { display:flex; gap:11px; align-items:flex-start; margin-top:16px; }
  .auth-feat .ic { width:34px; height:34px; border-radius:10px; flex:none; display:flex; align-items:center; justify-content:center; background:rgba(255,255,255,.08); font-size:16px; }
  .auth-form { display:flex; align-items:center; justify-content:center; padding:40px 24px; }
  .auth-card { width:100%; max-width:400px; }
  .auth-card h1 { font-size:1.8rem; }
  .mail-ico { width:64px; height:64px; border-radius:var(--r-lg); display:flex; align-items:center; justify-content:center; background:var(--brand-soft); color:var(--brand-bright); margin-bottom:18px; }
  .pw-meter { height:6px; border-radius:99px; background:var(--surface-3); overflow:hidden; margin-top:8px; }
  .pw-meter > i { display:block; height:100%; width:0; border-radius:99px; background:var(--danger); transition:width .25s, background .25s; }
  @media (max-width: 860px){ .auth { grid-template-columns:1fr; } .auth-brand { display:none; } }
` }) %>

<div class="auth">
  <!-- BRAND PANEL -->
  <div class="auth-brand">
    <a class="auth-logo" href="/">
      <svg viewBox="0 0 48 48" fill="none" style="width:38px;height:38px">
        <defs><linearGradient id="tlg-a" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stop-color="#9d83ff"/><stop offset="1" stop-color="#5b3fd6"/></linearGradient></defs>
        <path d="M24 4 L40 9 V23 C40 33 33 40 24 44 C15 40 8 33 8 23 V9 Z" fill="url(#tlg-a)" stroke="#9d83ff" stroke-width="1.2"/>
        <path d="M15 28 L24 17 L33 28" fill="none" stroke="#0d1124" stroke-width="3.4" stroke-linecap="round" stroke-linejoin="round"/>
        <path d="M19 33 L24 27 L29 33" fill="none" stroke="#facc15" stroke-width="3.2" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
      <span style="font-family:var(--font-display);font-weight:700;font-size:22px;color:#fff">Tiper<b style="color:var(--gold)">liga</b></span>
    </a>
    <div>
      <div class="auth-tagline">Skoro hotovo.<br>Nastav si nové heslo.</div>
      <div style="max-width:380px;margin-top:24px">
        <div class="auth-feat"><span class="ic">🔒</span><div><b style="color:#fff">Silné heslo</b><div style="color:rgba(255,255,255,.6);font-size:var(--fs-sm)">Vyber si heslo, ktoré nepoužívaš inde.</div></div></div>
        <div class="auth-feat"><span class="ic">⚡</span><div><b style="color:#fff">Hotovo za minútu</b><div style="color:rgba(255,255,255,.6);font-size:var(--fs-sm)">Potom sa rovno prihlásiš s novým heslom.</div></div></div>
      </div>
    </div>
    <div style="color:rgba(255,255,255,.4);font-size:var(--fs-sm)">© 2026 Tiperliga</div>
  </div>

  <!-- FORM -->
  <div class="auth-form">
    <div class="auth-card">

      <% if (typeof done !== 'undefined' && done) { %>
      <!-- HOTOVO: heslo zmenené -->
      <div class="mail-ico" style="background:var(--success-soft);color:var(--success)"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" style="width:30px;height:30px"><path d="M20 6L9 17l-5-5" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
      <h1>Heslo zmenené 🎉</h1>
      <p class="muted" style="margin:8px 0 24px">Tvoje heslo bolo úspešne aktualizované. Teraz sa môžeš prihlásiť s novým heslom.</p>
      <a href="/login" class="btn btn-primary btn-lg btn-block">Prihlásiť sa</a>

      <% } else if (!validToken) { %>
      <!-- NEPLATNÝ / EXPIROVANÝ ODKAZ -->
      <div class="mail-ico" style="background:var(--danger-soft);color:var(--danger)"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" style="width:30px;height:30px"><path d="M18 6 6 18M6 6l12 12" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
      <h1>Odkaz nie je platný</h1>
      <p class="muted" style="margin:8px 0 24px">Tento odkaz na obnovu hesla je neplatný alebo už expiroval. Požiadaj o nový odkaz.</p>
      <a href="/forgot-password" class="btn btn-primary btn-lg btn-block">Požiadať o nový odkaz</a>
      <p class="muted center" style="margin-top:22px;font-size:var(--fs-sm)"><a href="/login" class="brand" style="font-weight:700">← Späť na prihlásenie</a></p>

      <% } else { %>
      <!-- FORMULÁR NA NOVÉ HESLO -->
      <h1>Nastav nové heslo</h1>
      <p class="muted" style="margin:8px 0 24px">Vyber si silné heslo, ktoré nepoužívaš na iných stránkach.</p>
      <% if (typeof error !== 'undefined' && error) { %>
        <div class="card card-pad" style="background:var(--danger-soft);color:var(--danger);padding:12px 14px;margin-bottom:16px;font-size:var(--fs-sm)"><%= error %></div>
      <% } %>
      <!-- Reálne odoslanie na backend (POST /reset-password). Token putuje v skrytom poli. -->
      <form method="POST" action="/reset-password" onsubmit="return validateReset()">
        <input type="hidden" name="token" value="<%= token %>">
        <div class="field" style="margin-bottom:16px"><label class="label">Nové heslo</label>
          <input class="input" type="password" id="np" name="password" placeholder="••••••••" required oninput="meter()">
          <div class="pw-meter"><i id="pwBar"></i></div>
          <span class="hint" id="pwHint">Min. 8 znakov, kombinuj písmená a čísla.</span>
        </div>
        <div class="field" style="margin-bottom:22px"><label class="label">Potvrdiť heslo</label><input class="input" type="password" id="np2" name="confirmPassword" placeholder="••••••••" required></div>
        <button class="btn btn-primary btn-lg btn-block" type="submit">Uložiť nové heslo</button>
      </form>
      <% } %>

    </div>
  </div>
</div>

<script>
  // Sila hesla (jednoduchý vizuálny meter).
  function meter(){
    var v = document.getElementById('np').value || '';
    var score = 0;
    if (v.length >= 8) score++;
    if (/[A-Z]/.test(v)) score++;
    if (/[0-9]/.test(v)) score++;
    if (/[^A-Za-z0-9]/.test(v)) score++;
    var bar = document.getElementById('pwBar');
    var hint = document.getElementById('pwHint');
    var pct = [10, 35, 60, 80, 100][score];
    var col = score <= 1 ? 'var(--danger)' : score === 2 ? 'var(--warning)' : score === 3 ? 'var(--brand)' : 'var(--success)';
    bar.style.width = pct + '%';
    bar.style.background = col;
    hint.textContent = score <= 1 ? 'Slabé heslo — pridaj dĺžku a znaky.' : score === 2 ? 'Priemerné heslo.' : score === 3 ? 'Dobré heslo.' : 'Silné heslo 💪';
  }

  // Klientská kontrola pred odoslaním (backend ju validuje znova — autoritatívne).
  function validateReset(){
    var a = (document.getElementById('np') || {}).value || '';
    var b = (document.getElementById('np2') || {}).value || '';
    var hint = document.getElementById('pwHint');
    if (a.length < 8) { if (hint) hint.textContent = 'Heslo musí mať aspoň 8 znakov.'; return false; }
    if (a !== b) { if (hint) hint.textContent = 'Heslá sa nezhodujú.'; return false; }
    return true;
  }
</script>
<script src="/js/enhance.js"></script>
</body>
</html>
